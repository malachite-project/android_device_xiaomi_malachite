#############################################
#           init.insmod.cfg                 #
# This file contains kernel modules to load #
# at init time by init.insmod.sh script     #
#############################################

# Load kernel modules here, add your KO behind
modprobe|*

# All load attempts finished, including failures. Keep the boot handoff;
# init.insmod.sh exits nonzero if any attempt failed.
setprop|vendor.all.modules.ready
